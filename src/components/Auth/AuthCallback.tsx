// Legacy file retained temporarily during feature-slice migration.
// Re-export new location to avoid breaking imports still referencing old path.
export { default } from '../../features/auth/components/AuthCallback';
